- name: Install php5
  sudo: yes
  apt: pkg=php5 state=latest

-   name: Setup php.ini
    sudo: yes
    lineinfile: dest="/etc/php5/apache2/php.ini"
                regexp="{{ item.reg }}"
                line="{{ item.line }}"
    with_items: php_ini_settings
    